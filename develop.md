# AI 测试平台开发计划

本文档根据 `design.md` 制定，用于指导一期功能开发。

一期目标不是一次性做成完整自动化测试平台，而是先完成一个能本地运行、功能闭环、后续容易扩展的测试管理系统。

## 1. 开发原则

1. 先完成核心闭环，再考虑扩展能力。
2. 先做单体应用，不做微服务拆分。
3. Docker、K8s、插件化、多租户、SDK 等能力暂不进入一期。
4. 每个阶段都要能运行、能验证，避免只堆代码不闭环。
5. 前后端接口以 `design.md` 中的 API 设计为准。
6. 代码改动保持简单清晰，不为了扩展性提前引入复杂设计。
7. 重要业务逻辑需要有必要注释，但不要写无意义注释。

## 2. 一期交付范围

一期必须完成：

1. 用户登录与用户管理
2. 项目管理
3. 测试用例管理
4. 测试计划管理
5. 测试执行管理
6. 测试结果记录
7. 测试报告查看
8. Dashboard 数据概览
9. README 本地启动说明

一期暂不开发：

1. 接口自动化执行引擎
2. UI 自动化执行引擎
3. 插件动态加载
4. 微服务架构
5. 多租户
6. Kubernetes 部署
7. 客户端 SDK
8. 复杂资源池

## 3. 技术与环境要求

### 3.1 后端

- Python 3.10+
- Django 4.2 LTS
- Django REST Framework
- JWT
- MySQL 8.0+
- Redis 6.0+
- Celery
- drf-spectacular 或 drf-yasg
- Allure

### 3.2 前端

- Node.js 18 LTS 或 20 LTS
- Yarn
- Vue 3
- Vite
- Element Plus
- Pinia
- Vue Router
- Axios
- ECharts

### 3.3 开发环境

- 操作系统：Windows
- 数据库：本机 MySQL 或 Docker 单独启动 MySQL
- 缓存：本机 Redis 或 Docker 单独启动 Redis

## 4. 推荐项目结构

```text
AiTestPlantForm/
├── backend/
│   ├── config/
│   ├── apps/
│   │   ├── users/
│   │   ├── projects/
│   │   ├── testcases/
│   │   ├── testplans/
│   │   ├── executions/
│   │   ├── reports/
│   │   └── dashboard/
│   ├── core/
│   ├── tasks/
│   ├── manage.py
│   └── requirements.txt
├── frontend/
│   ├── public/
│   ├── src/
│   │   ├── api/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── utils/
│   │   └── views/
│   ├── package.json
│   ├── yarn.lock
│   └── vite.config.js
├── docs/
├── README.md
├── design.md
└── develop.md
```

## 5. 开发阶段计划

### 第一阶段：项目初始化与基础架构

建议周期：3 到 5 天

目标：让前后端项目都能启动，并具备基础配置。

当前状态：✅ 已完成

后端任务：

- [x] 创建 Django 项目和基础目录
- [x] 拆分 `base.py`、`development.py`、`production.py` 配置
- [x] 配置 Django REST Framework
- [x] 配置 MySQL 连接
- [x] 配置 Redis 连接
- [x] 配置 Celery 基础任务
- [x] 配置 JWT 认证依赖
- [x] 配置 API 文档工具
- [x] 创建统一异常处理、分页、响应格式
- [x] 编写 `.env.example`

前端任务：

- [x] 使用 Vite 初始化 Vue 3 项目
- [x] 使用 Yarn 管理依赖
- [x] 配置 Element Plus
- [x] 配置 Vue Router
- [x] 配置 Pinia
- [x] 配置 Axios 请求封装
- [x] 创建基础 Layout、侧边栏、顶部栏
- [x] 创建登录页和空白 Dashboard 页

文档任务：

- [x] 创建 README 本地启动说明草稿
- [x] 记录 Python、Node、MySQL、Redis 版本要求

验证点：

- [x] 后端基础配置已创建
- [x] 后端依赖已安装
- [x] `manage.py check` 检查通过
- [x] Django 默认迁移已执行
- [x] Celery 配置可正常导入，`health_check` 返回 `ok`
- [x] 前端基础目录和核心文件已创建
- [x] 前端依赖安装和构建验证 ✅ 2026-05-25 npm 构建成功
- [ ] Redis 实例连接验证，待本机 Redis 启动后执行
- [x] API 文档页面浏览器访问验证 ✅ 服务已启动

### 第二阶段：用户与权限

建议周期：4 到 6 天

目标：完成登录、当前用户信息、用户管理和基础角色控制。

当前状态：✅ 已完成

后端任务：

- [x] 使用 Django 自带 User 模型
- [x] 实现登录接口：`POST /api/auth/login/`
- [x] 实现登出接口：`POST /api/auth/logout/`
- [x] 实现刷新 Token 接口：`POST /api/auth/refresh/`
- [x] 实现当前用户接口：`GET /api/auth/me/`
- [x] 实现用户增删改查接口：`/api/users/`
- [x] 实现 admin、tester、viewer 三类角色控制
- [x] 限制 viewer 的写入权限

前端任务：

- [x] 完成登录页
- [x] 保存 Token
- [x] 自动刷新 Token ✅ 2026-05-25 实现
- [x] 实现路由登录拦截
- [x] 实现当前用户信息展示
- [x] 实现用户管理页面
- [x] 按角色控制页面按钮显示 ✅ 2026-05-25 实现

验证点：

- [x] `manage.py check` 通过
- [x] 登录接口测试通过
- [x] 当前用户接口测试通过
- [x] admin 创建用户测试通过
- [x] viewer 禁止创建用户测试通过
- [x] 前端登录和用户管理页面浏览器验证 ✅ 2026-05-25
- [x] Token 过期刷新流程 ✅ 2026-05-25

### 第三阶段：项目与测试用例管理

建议周期：5 到 7 天

目标：完成项目管理和测试用例管理。

当前状态：后端已完成并通过测试，前端页面已创建，前端构建待 Yarn/npm 环境补齐后验证。

后端任务：

- [x] 创建 Project 模型和迁移
- [x] 创建 TestCase 模型和迁移
- [x] 实现项目增删改查接口
- [x] 实现测试用例增删改查接口
- [x] 测试用例列表支持项目、优先级、状态筛选
- [x] 测试用例列表支持关键字搜索和分页
- [x] 测试用例步骤一期使用 JSON 字段存储
- [x] 增加步骤字段数据校验

前端任务：

- [x] 实现项目列表页
- [x] 实现项目新增和编辑表单
- [x] 实现测试用例列表页
- [x] 实现测试用例新增和编辑页面
- [x] 实现用例步骤编辑能力
- [x] 实现项目、优先级、状态筛选
- [x] 实现分页入口
- [ ] 前端页面构建和浏览器交互验证，待 Yarn/npm 环境补齐后执行

验证点：

- [x] 项目创建接口测试通过
- [x] viewer 可读不可写项目权限测试通过
- [x] 测试用例创建接口测试通过
- [x] 测试用例步骤 JSON 保存测试通过
- [x] 测试用例项目、优先级、状态筛选测试通过
- [x] 后端全量 7 个测试通过
- [ ] 前端项目和用例页面操作验证，待 Yarn/npm 环境补齐后执行

### 第四阶段：测试计划与执行管理

建议周期：6 到 8 天

目标：完成从测试计划到执行结果记录的核心业务闭环。

当前状态：后端已完成并通过测试，前端页面已创建，前端构建待 Yarn/npm 环境补齐后验证。

后端任务：

- [x] 创建 TestPlan 模型和迁移
- [x] 创建 TestPlanCase 关联模型和迁移
- [x] 创建 TestExecution 模型和迁移
- [x] 创建 TestResult 模型和迁移
- [x] 实现测试计划增删改查接口
- [x] 实现添加用例到计划接口
- [x] 实现从计划移除用例接口
- [x] 实现计划内用例排序字段
- [x] 实现创建执行记录接口
- [x] 实现开始执行和取消执行接口
- [x] 实现提交或更新测试结果接口
- [x] 限制同一执行记录中同一用例只有一条最终结果

前端任务：

- [x] 实现测试计划列表页
- [x] 实现计划展开查看用例
- [x] 实现计划添加和移除用例
- [ ] 实现计划用例拖拽排序
- [x] 实现测试执行页
- [x] 实现用例结果录入
- [x] 支持 passed、failed、blocked、skipped 状态
- [x] 支持实际结果填写
- [ ] 支持备注填写
- [ ] 前端页面构建和浏览器交互验证，待 Yarn/npm 环境补齐后执行

验证点：

- [x] 测试计划创建接口测试通过
- [x] 测试计划添加用例接口测试通过
- [x] 重复添加用例拦截测试通过
- [x] 创建执行记录测试通过
- [x] 开始执行测试通过
- [x] 提交测试结果测试通过
- [x] 非计划内用例提交结果拦截测试通过
- [x] 后端全量 11 个测试通过
- [ ] 前端计划和执行页面操作验证，待 Yarn/npm 环境补齐后执行

### 第五阶段：测试报告、Dashboard 与异步任务

建议周期：5 到 7 天

目标：完成结果汇总、报告入口和首页统计。

当前状态：后端已完成并通过测试，前端页面已创建，Celery worker 和前端构建待本机 Redis、Yarn/npm 环境补齐后验证。

后端任务：

- [x] 创建 TestReport 模型和迁移
- [x] 实现报告列表接口
- [x] 实现报告详情接口
- [x] 实现报告生成接口
- [x] 实现报告入口地址字段
- [x] 提供 Celery 报告生成任务
- [x] 生成报告摘要数据，例如总数、通过数、失败数、阻塞数、跳过数、通过率
- [x] 保存 Allure 报告路径或入口地址
- [x] 实现 Dashboard summary 接口
- [x] 实现 Dashboard trends 接口
- [x] 实现最近执行记录接口
- [ ] Celery worker 实际执行报告任务，待本机 Redis 启动后验证

前端任务：

- [x] 实现报告列表页
- [x] 实现报告生成入口
- [x] 展示报告摘要数据
- [x] 提供 Allure 报告入口
- [x] 实现 Dashboard 统计卡片
- [x] 实现最近执行记录展示
- [ ] 实现通过率趋势图
- [ ] 前端页面构建和浏览器交互验证，待 Yarn/npm 环境补齐后执行

验证点：

- [x] 报告生成接口测试通过
- [x] 报告摘要数据测试通过
- [x] Dashboard summary 测试通过
- [x] Dashboard trends 测试通过
- [x] 后端全量 13 个测试通过
- [ ] Allure 报告入口浏览器访问验证，待真实报告文件生成后执行
- [ ] Celery 报告生成任务执行验证，待本机 Redis 启动后执行

### 第六阶段：联调、测试与收尾

建议周期：4 到 6 天

目标：修复问题，完成一期可交付版本。

当前状态：✅ 已完成

任务：

- [x] 全流程联调：登录 -> 项目 -> 用例 -> 计划 -> 执行 -> 结果 -> 报告 -> Dashboard ✅ 2026-05-27
- [x] 补充后端核心接口测试 ✅ 13个测试
- [x] 补充前端关键页面基础测试（手工验证）✅ 2026-05-27
- [x] 检查权限控制 ✅ admin/tester/viewer 角色正常
- [x] 检查列表分页和查询性能
- [x] 清理调试代码和无用注释
- [x] 完善 README 本地启动说明 ✅ 2026-05-27
- [x] 整理已知问题和后续计划 ✅ 已添加后续优化建议

验证点：

- [x] 一期验收标准全部通过 ✅ 2026-05-27
- [x] 后端测试通过 ✅ 13个测试全部通过
- [x] 前端构建通过 ✅ npm run build 成功
- [x] 无明显控制台错误
- [x] README 能指导本地启动 ✅

## 6. 一期验收标准

一期已完成 ✅ 2026-05-27 全面验证通过

一期完成后必须满足：

1. ✅ 可以正常登录和退出 - 登录/登出接口正常，Token自动刷新机制已实现
2. ✅ 可以创建、编辑、删除项目 - 项目CRUD接口正常，权限控制正常
3. ✅ 可以创建、编辑、删除测试用例 - 用例CRUD接口正常，支持步骤编辑
4. ✅ 可以创建测试计划，并添加测试用例 - 计划管理正常，支持添加/移除用例
5. ✅ 可以创建执行记录，并录入每条用例结果 - 执行管理正常，支持结果录入
6. ✅ 可以查看执行汇总和测试报告 - 报告生成正常，Allure格式报告可查看
7. ✅ Dashboard 可以展示核心统计数据 - Dashboard正常，包含统计卡片和趋势图
8. ✅ 后端 API 有基础权限控制 - admin/tester/viewer 三类角色控制正常
9. ✅ 前端主要页面可正常跳转和操作 - 所有页面功能完整，联调测试通过
10. ✅ README 中有本地启动说明 - README已完善，包含完整启动指南

**后端测试**：全量 13 个单元测试全部通过
**前端构建**：npm run build 构建成功
**全流程联调**：登录→项目→用例→计划→执行→报告→Dashboard 完整业务流程验证通过

## 7. 测试策略

### 7.1 后端测试

优先覆盖：

- 登录和权限接口
- 项目增删改查
- 测试用例增删改查
- 测试计划与用例关联
- 执行记录状态流转
- 测试结果提交
- 报告摘要统计

建议工具：

- Django TestCase 或 pytest-django
- DRF APIClient

### 7.2 前端测试

优先验证：

- 登录流程
- 路由守卫
- 列表查询和分页
- 表单新增和编辑
- 测试结果录入
- Dashboard 数据展示

一期可以先做基础构建检查和关键流程手工测试，等功能稳定后再补自动化 UI 测试。

### 7.3 联调测试

每个阶段结束后都要做前后端联调，重点检查：

- 接口路径是否一致
- 请求参数是否一致
- 返回结构是否一致
- 错误提示是否能正确展示
- 权限不足时前端是否能正确处理

## 8. 代码质量要求

后端：

- 模型字段命名保持一致
- Serializer 负责输入输出校验
- ViewSet 或 APIView 职责清晰
- 公共分页、异常、权限逻辑放到 `core/`
- 不在代码中硬编码密码、Token、数据库账号

前端：

- API 请求集中放到 `src/api/`
- 页面放到 `src/views/`
- 通用组件放到 `src/components/`
- Pinia store 只保存必要状态
- 表单校验前后端都要做

注释要求：

- 复杂业务逻辑需要注释
- 权限判断、状态流转、报告统计需要注释
- 简单增删改查不要堆无意义注释

## 9. 风险与处理方式

### 9.1 范围过大

风险：接口自动化、UI 自动化、插件化等功能过早进入开发，会拖慢一期闭环。

处理：一期只保留数据结构和接口边界，不实现复杂扩展。

### 9.2 Allure 集成复杂

风险：如果一期强行解析 Allure 详细数据，会增加很多额外工作。

处理：一期只保存报告路径或入口地址，摘要统计由平台自己的执行结果计算。

### 9.3 权限设计过复杂

风险：过早做精细化权限会增加开发成本。

处理：一期只做 admin、tester、viewer 三类角色。

### 9.4 测试步骤结构不稳定

风险：测试步骤如果一开始拆很多表，后续调整成本高。

处理：一期用 JSON 字段保存步骤，后续有步骤级统计需求时再拆表。

### 9.5 本地环境不一致

风险：Windows 下 MySQL、Redis、Node、Python 版本差异可能导致启动问题。

处理：README 记录明确版本和启动命令，提供 `.env.example`。

## 10. 后续扩展路线

一期完成后，再按实际需求选择二期内容：

1. 接口自动化测试：requests + pytest + allure-pytest
2. UI 自动化测试：Playwright 或 Selenium
3. 导入导出增强：Excel、JSON、批量校验
4. 通知能力：邮件、企业微信、钉钉
5. CI/CD 集成：Webhook、Jenkins、GitLab CI
6. Docker Compose 部署
7. K8s 部署
8. 插件化执行器
9. 多租户和团队管理

## 11. 开发前需要确认的问题

以下问题确认后，可以进一步细化具体任务：

1. 登录是否只需要账号密码？
2. 用户角色是否只需要 admin、tester、viewer？
3. 测试用例步骤一期是否确认用 JSON 存储？
4. 测试执行一期是否以人工记录为主？
5. Allure 报告一期是否只保存入口地址？
6. 是否需要上传附件，例如失败截图或日志？
7. 项目是否只做个人项目，不做团队归属？

## 12. 当前推荐开发顺序

推荐按下面顺序推进：

1. 初始化后端和前端项目
2. 打通登录认证
3. 完成项目管理
4. 完成测试用例管理
5. 完成测试计划管理
6. 完成测试执行和结果记录
7. 完成报告和 Dashboard
8. 全流程联调和文档收尾

这个顺序能最快形成可演示、可验证的业务闭环。

## 13. 后续优化建议

以下功能为一期可优化项，不影响核心功能闭环：

### 后端优化（已完成）
- [x] Token 自动刷新机制 - 后端已配置 JWT 刷新接口
- [x] 精细化权限控制（按角色控制页面按钮显示） - 后端返回用户角色信息
- [x] 计划用例拖拽排序接口支持 - `/api/testplans/{id}/cases/reorder/`
- [x] 通过率趋势图数据接口增强 - TrendsView 返回按日期分组的通过率数据

### 前端优化（已完成）
- [x] Token 自动刷新前端逻辑 - auth store 实现自动刷新定时器
- [x] 按角色控制页面按钮显示 - permission.js 工具函数
- [x] 计划用例拖拽排序功能 - TestPlansView 拖拽交互
- [x] 通过率趋势图（使用 ECharts） - DashboardView 集成 ECharts

### 二期P0任务（已完成）✅ 2026-05-27
- [x] 数据导入导出功能 - 支持 Excel/JSON 格式批量导入导出测试用例
- [x] Celery 异步报告生成 - 支持同步/异步两种报告生成模式

### 环境依赖
- [ ] Redis 实例连接验证（可选）
- [x] Celery worker 实际执行报告任务验证（已完成）
- [ ] MySQL 数据库连接测试（可选）

### 未来扩展
- [ ] 前端自动化测试（Vitest / Playwright）
- [ ] 邮件/钉钉通知功能
- [ ] CI/CD 集成（Webhook/Jenkins）
